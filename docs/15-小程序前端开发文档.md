# 十五、小程序前端开发文档

> 本文档记录 uni-app 小程序前端的实现详情。

---

## 1. 项目结构

```
/
├── api/                        # API 接口封装
│   ├── auth.ts                 # 认证接口
│   ├── user.ts                 # 用户接口
│   ├── health.ts               # 健康筛查接口
│   ├── sport.ts                # 运动接口
│   ├── food.ts                 # 饮食接口
│   ├── shop.ts                 # 商城/购物车/订单/优惠券接口
│   ├── community.ts            # 社区接口
│   ├── message.ts              # 私信/通知接口
│   ├── points.ts               # 积分/任务/签到接口
│   ├── member.ts               # 会员接口
│   ├── doctor.ts               # 远程医疗接口
│   ├── ai.ts                   # AI 助手接口
│   ├── home.ts                 # 首页接口
│   └── index.ts                # 统一导出
│
├── components/                 # 自定义组件
│   ├── gender-selector.vue     # 性别选择器
│   ├── ruler-slider.vue        # 刻度尺滑块
│   └── ...
│
├── pages/                      # 页面
│   ├── index/                  # 首页
│   ├── login/                  # 登录
│   ├── health/                 # 健康筛查
│   │   ├── index.vue           # 筛查历史
│   │   ├── screening.vue       # 筛查流程
│   │   └── result.vue          # 筛查结果
│   ├── sport/                  # 运动
│   │   ├── index.vue           # 运动记录页
│   │   └── history.vue         # 运动记录历史
│   ├── food/                   # 饮食
│   │   └── index.vue           # 饮食记录页
│   ├── community/              # 社区
│   │   ├── index.vue           # 社区首页
│   │   ├── detail.vue          # 动态详情
│   │   ├── publish.vue         # 发布动态
│   │   └── topic.vue           # 话题详情
│   ├── shop/                   # 商城
│   │   ├── index.vue           # 商城首页
│   │   ├── detail.vue          # 商品详情
│   │   └── collect.vue         # 收藏列表
│   ├── cart/                   # 购物车
│   │   └── index.vue           # 购物车页面
│   ├── order/                  # 订单
│   │   ├── index.vue           # 订单列表
│   │   ├── confirm.vue         # 订单确认
│   │   └── detail.vue          # 订单详情
│   ├── user/                   # 个人中心
│   │   ├── index.vue           # 个人中心首页
│   │   ├── profile.vue         # 个人资料编辑
│   │   ├── homepage.vue        # 他人主页
│   │   ├── member.vue          # 会员中心
│   │   ├── points.vue          # 积分明细
│   │   ├── settings.vue        # 设置页面
│   │   ├── account-security.vue # 账号与安全
│   │   ├── privacy-settings.vue # 隐私设置
│   │   ├── about.vue           # 关于我们
│   │   └── feedback.vue        # 意见反馈
│   ├── address/                # 地址管理
│   │   ├── index.vue           # 地址列表
│   │   └── add-edit.vue        # 新增/编辑地址
│   ├── coupon/                 # 优惠券
│   │   ├── index.vue           # 我的优惠券
│   │   └── center.vue          # 领券中心
│   ├── reminder/               # 智能提醒
│   │   ├── index.vue           # 提醒列表
│   │   └── add-edit.vue        # 新增/编辑提醒
│   ├── doctor/                 # 医生咨询
│   │   ├── index.vue           # 医生列表
│   │   ├── detail.vue          # 医生详情
│   │   ├── chat.vue            # 在线咨询
│   │   └── article.vue         # 健康科普
│   ├── message/                # 消息中心
│   │   ├── index.vue           # 消息列表
│   │   └── chat.vue            # 私信聊天
│   ├── ai/                     # AI 助手
│   │   └── index.vue           # AI 对话
│   ├── onboarding/             # 引导页
│   │   └── welcome.vue         # 欢迎引导页
│   └── help/                   # 帮助中心
│       └── index.vue           # 帮助与反馈
│
├── stores/                     # Pinia 状态管理
│   ├── index.ts                # Pinia 实例
│   └── user.ts                 # 用户状态
│
├── static/                     # 静态资源
│   └── placeholder/            # 占位图
│
├── utils/                      # 工具函数
│   └── request.ts              # 请求封装
│
├── App.vue                     # 根组件
├── main.ts                     # 入口文件
├── manifest.json               # 应用配置
├── pages.json                  # 页面配置
└── uni.scss                    # 全局样式变量
```

---

## 2. 技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| uni-app | 3.x | 跨端框架 |
| Vue | 3.x | 前端框架 |
| TypeScript | 5.x | 类型安全 |
| Pinia | 2.x | 状态管理 |
| Wot Design Uni | 1.x | UI 组件库 |
| dayjs | 1.x | 日期处理 |

---

## 3. 请求封装

### 3.1 配置 (utils/request.ts)

```typescript
// 基础配置
const BASE_URL = 'http://localhost:8000/api/v1'
const USE_MOCK = false // 关闭 Mock 模式

// 请求拦截
function request(options) {
  const token = uni.getStorageSync('token')
  return new Promise((resolve, reject) => {
    uni.request({
      url: BASE_URL + options.url,
      method: options.method || 'GET',
      data: options.data,
      header: {
        'Authorization': token ? `Bearer ${token}` : '',
        'Content-Type': 'application/json'
      },
      success: (res) => {
        if (res.data.code === 200) {
          resolve(res.data)
        } else if (res.data.code === 401) {
          // Token 失效，跳转登录
          uni.reLaunch({ url: '/pages/login/index' })
          reject(res.data)
        } else {
          uni.showToast({ title: res.data.message, icon: 'none' })
          reject(res.data)
        }
      },
      fail: reject
    })
  })
}
```

---

## 4. 状态管理

### 4.1 用户状态 (stores/user.ts)

```typescript
export const useUserStore = defineStore('user', () => {
  // State
  const userInfo = ref<UserInfo | null>(null)
  const token = ref<string>(uni.getStorageSync('token') || '')

  // Getters
  const isLoggedIn = computed(() => !!token.value)
  const nickname = computed(() => userInfo.value?.nickname || '游客')
  const sportCoins = computed(() => userInfo.value?.sport_coins || 0)
  const foodCoins = computed(() => userInfo.value?.food_coins || 0)
  const isVip = computed(() => {
    if (!userInfo.value?.member_expire_time) return false
    return new Date(userInfo.value.member_expire_time).getTime() > Date.now()
  })

  // Actions
  async function login(code: string) { ... }
  async function fetchUserInfo() { ... }
  function logout() { ... }

  return { ... }
})
```

---

## 5. API 模块

### 5.1 认证 (api/auth.ts)

```typescript
// 微信登录
export function wxLogin(code: string) {
  return request.post('/auth/login', { code })
}

// 完善用户信息
export function register(data: any) {
  return request.post('/auth/register', data)
}
```

### 5.2 用户 (api/user.ts)

```typescript
// 获取用户信息
export function getUserProfile() {
  return request.get('/user/profile')
}

// 更新用户信息
export function updateUserProfile(data: any) {
  return request.put('/user/profile', data)
}

// 获取健康档案
export function getUserHealthProfile() {
  return request.get('/user/health-profile')
}
```

### 5.3 健康筛查 (api/health.ts)

```typescript
// 提交筛查数据
export function submitScreening(data: any) {
  return request.post('/health/screening', data)
}

// 获取筛查记录
export function getScreeningRecords(params?: any) {
  return request.get('/health/records', params)
}

// 获取报告详情
export function getScreeningReport(id: number) {
  return request.get(`/health/report/${id}`)
}
```

### 5.4 商城 (api/shop.ts)

```typescript
// 商品列表
export function getProducts(params?: any) {
  return request.get('/shop/products', params)
}

// 购物车
export function getCartList() {
  return request.get('/cart')
}

// 创建订单
export function createOrder(data: any) {
  return request.post('/order', data)
}

// 支付订单
export function payOrder(id: number) {
  return request.post(`/order/${id}/pay`)
}
```

### 5.5 社区 (api/community.ts)

```typescript
// 动态列表
export function getPosts(params?: any) {
  return request.get('/community/posts', params)
}

// 发布动态
export function publishPost(data: any) {
  return request.post('/community/post', data)
}

// 点赞
export function likePost(id: number) {
  return request.post(`/community/post/${id}/like`)
}

// 关注用户
export function followUser(userId: number) {
  return request.post(`/community/follow/${userId}`)
}
```

### 5.6 积分 (api/points.ts)

```typescript
// 积分余额
export function getPointsBalance() {
  return request.get('/points/balance')
}

// 每日签到
export function dailyCheckin() {
  return request.post('/checkin')
}

// 领取任务奖励
export function claimTaskReward(taskId: number) {
  return request.post(`/task/${taskId}/claim`)
}
```

### 5.7 运动 (api/sport.ts)

```typescript
// 提交运动记录
export function submitSportRecord(data: any) {
  return request.post('/sport/record', data)
}

// 获取运动记录列表
export function getSportRecords(params?: any) {
  return request.get('/sport/records', params)
}

// 运动打卡
export function sportCheckin() {
  return request.post('/sport/checkin')
}
```

### 5.8 饮食 (api/food.ts)

```typescript
// 提交饮食记录
export function submitFoodRecord(data: any) {
  return request.post('/food/record', data)
}

// 获取饮食记录列表
export function getFoodRecords(params?: any) {
  return request.get('/food/records', params)
}

// 搜索食物库
export function searchFoodLibrary(params?: any) {
  return request.get('/food-library/search', params)
}
```

### 5.9 AI 助手 (api/ai.ts)

```typescript
// AI 对话
export function aiChat(message: string) {
  return request.post('/ai/chat', { message })
}

// 获取对话历史
export function getAiHistory(params?: any) {
  return request.get('/ai/history', params)
}

// 删除对话历史
export function deleteAiHistory(sessionId: string) {
  return request.delete(`/ai/history/${sessionId}`)
}
```

### 5.10 通知消息 (api/notification.ts)

```typescript
// 获取通知列表
export function getNotifications(params?: any) {
  return request.get('/notification/list', params)
}

// 标记消息已读
export function markNotificationRead(id: number) {
  return request.post('/notification/read', { ids: [id] })
}

// 全部已读
export function markAllNotificationRead() {
  return request.post('/notification/read-all')
}

// 获取未读数量
export function getUnreadCount() {
  return request.get('/notification/unread-count')
}
```

### 5.11 订单 (api/order.ts)

```typescript
// 获取订单列表
export function getOrders(params?: any) {
  return request.get('/order/list', params)
}

// 获取订单详情
export function getOrderDetail(id: number) {
  return request.get(`/order/${id}`)
}

// 创建订单
export function createOrder(data: any) {
  return request.post('/order', data)
}

// 支付订单
export function payOrder(id: number, payType: string) {
  return request.post(`/order/${id}/pay`, { pay_type: payType })
}

// 取消订单
export function cancelOrder(id: number) {
  return request.post(`/order/${id}/cancel`)
}

// 确认收货
export function confirmOrder(id: number) {
  return request.post(`/order/${id}/confirm`)
}
```

### 5.12 购物车 (api/cart.ts)

```typescript
// 获取购物车列表
export function getCartList() {
  return request.get('/cart')
}

// 添加商品到购物车
export function addToCart(productId: number, quantity: number, specId?: number) {
  return request.post('/cart', { product_id: productId, quantity, spec_id: specId })
}

// 更新购物车商品数量
export function updateCartItem(id: number, quantity: number) {
  return request.put(`/cart/${id}`, { quantity })
}

// 删除购物车商品
export function deleteCartItem(id: number) {
  return request.delete(`/cart/${id}`)
}
```

### 5.13 地址 (api/address.ts)

```typescript
// 获取地址列表
export function getAddressList() {
  return request.get('/address/list')
}

// 添加地址
export function addAddress(data: any) {
  return request.post('/address', data)
}

// 更新地址
export function updateAddress(id: number, data: any) {
  return request.put(`/address/${id}`, data)
}

// 删除地址
export function deleteAddress(id: number) {
  return request.delete(`/address/${id}`)
}

// 设为默认地址
export function setDefaultAddress(id: number) {
  return request.put(`/address/${id}/default`)
}
```

### 5.14 提醒 (api/reminder.ts)

```typescript
// 获取提醒列表
export function getReminders() {
  return request.get('/reminder/list')
}

// 添加提醒
export function addReminder(data: any) {
  return request.post('/reminder', data)
}

// 更新提醒
export function updateReminder(id: number, data: any) {
  return request.put(`/reminder/${id}`, data)
}

// 删除提醒
export function deleteReminder(id: number) {
  return request.delete(`/reminder/${id}`)
}
```

### 5.15 用户设置 (api/user.ts - 设置相关)

```typescript
// 获取用户设置
export function getUserSettings() {
  return request.get('/settings')
}

// 更新用户设置
export function updateUserSettings(data: {
  share_health_data?: number
  public_profile?: number
  personalized?: number
  elderly_mode?: number
  notification_enabled?: number
}) {
  return request.put('/settings', data)
}

// 发送验证码
export function sendVerificationCode(phone: string) {
  return request.post(`/settings/send-code?phone=${phone}`)
}

// 绑定手机号
export function bindPhone(data: { phone: string; code: string }) {
  return request.post('/settings/bind-phone', data)
}

// 解绑手机号
export function unbindPhone() {
  return request.post('/settings/unbind-phone')
}

// 注销账号
export function deleteAccount() {
  return request.post('/settings/delete-account')
}
```

### 5.16 用户反馈 (api/user.ts - 反馈相关)

```typescript
// 提交反馈
export function submitFeedback(data: {
  content: string
  images?: string[]
  contact?: string
}) {
  return request.post('/feedback', data)
}

// 获取我的反馈列表
export function getFeedbackList(params?: { page?: number; page_size?: number }) {
  return request.get('/feedback/list', params)
}
```

---

## 6. 页面对接

### 6.1 登录流程

```vue
<script setup lang="ts">
import { wxLogin } from '@/api/auth'
import { getUserProfile } from '@/api/user'
import { useUserStore } from '@/stores/user'

const userStore = useUserStore()

async function handleLogin() {
  // 1. 获取微信登录 code
  const loginRes = await uni.login()
  
  // 2. 调用后端登录接口
  const res = await wxLogin(loginRes.code)
  userStore.setToken(res.data.token)
  
  // 3. 获取用户信息
  const userRes = await getUserProfile()
  userStore.setUserInfo(userRes.data)
  
  // 4. 跳转首页
  uni.switchTab({ url: '/pages/index/index' })
}
</script>
```

### 6.2 首页数据

```vue
<script setup lang="ts">
import { getPersonalDashboard, getBanners } from '@/api/home'

const healthData = ref({})
const banners = ref([])

async function fetchData() {
  const [dashboardRes, bannerRes] = await Promise.all([
    getPersonalDashboard(),
    getBanners({ position: 'home' })
  ])
  healthData.value = dashboardRes.data
  banners.value = bannerRes.data.list
}

onLoad(() => {
  fetchData()
})
</script>
```

### 6.3 商品列表

```vue
<script setup lang="ts">
import { getCategories, getProducts } from '@/api/shop'

const categories = ref([])
const products = ref([])

async function fetchCategories() {
  const res = await getCategories()
  categories.value = res.data.list
}

async function fetchProducts(params = {}) {
  const res = await getProducts(params)
  products.value = res.data.list
}

onMounted(() => {
  fetchCategories()
  fetchProducts()
})
</script>
```

---

## 7. 启动方式

### 7.1 开发环境

```bash
# 安装依赖
npm install

# 启动开发服务（微信小程序）
npm run dev:mp-weixin

# 使用微信开发者工具打开 dist/dev/mp-weixin 目录
```

### 7.2 生产构建

```bash
npm run build:mp-weixin
# 输出到 dist/build/mp-weixin
```

---

## 8. 设计规范

### 8.1 颜色系统（苹果风格）

```scss
// uni.scss
$primary-color: #0071e3;      // 主色调（蓝色）
$text-color: #1d1d1f;         // 主文字
$text-secondary: #86868b;     // 次要文字
$bg-color: #f5f5f7;           // 背景色
$border-color: #d2d2d7;       // 边框色
$success-color: #34c759;      // 成功
$warning-color: #ff9500;      // 警告
$danger-color: #ff3b30;       // 危险
```

### 8.2 间距规范

```scss
$spacing-xs: 8rpx;
$spacing-sm: 16rpx;
$spacing-md: 24rpx;
$spacing-lg: 32rpx;
$spacing-xl: 48rpx;
```

### 8.3 圆角规范

```scss
$radius-sm: 8rpx;
$radius-md: 16rpx;
$radius-lg: 24rpx;
$radius-xl: 32rpx;
```

---

## 9. 注意事项

1. **Token 管理**：使用 `uni.setStorageSync` 持久化存储
2. **页面跳转**：TabBar 页面使用 `switchTab`，普通页面使用 `navigateTo`
3. **图片处理**：上传前压缩，使用 WebP 格式
4. **性能优化**：列表使用虚拟滚动，图片懒加载
5. **错误处理**：统一使用 `uni.showToast` 提示

---

## 10. 暂缓功能

以下功能前端已预留UI，但后端暂未完全实现：

| 功能 | 前端 | 后端 | 备注 |
|------|------|------|------|
| GPS 轨迹 | 地图占位 | ❌ | 运动页显示mock |
| 微信支付 | 按钮可点 | Mock | 直接返回成功 |
| 食物识别 | 拍照入口 | ❌ | 预留接口 |
| 面部识别 | 拍照入口 | ❌ | 预留接口 |

---

## 11. 新增页面说明

### 11.1 运动记录历史页 (pages/sport/history.vue)

**功能**：
- 显示运动统计概览（次数、总时长、消耗卡路里）
- 按运动类型筛选（全部/户外跑/跑步机/健走）
- 运动记录列表展示（运动类型、时长、距离、卡路里、获得运动币）
- 下拉加载更多

**路由配置**：
```json
{
  "path": "pages/sport/history",
  "style": {
    "navigationBarTitleText": "运动记录"
  }
}
```

### 11.2 他人主页 (pages/user/homepage.vue)

**功能**：
- 展示用户公开信息（头像、昵称、简介）
- 显示关注数、粉丝数、获赞数
- 关注/取消关注操作
- 私信入口（暂缓）
- 用户发布的动态列表

**路由配置**：
```json
{
  "path": "pages/user/homepage",
  "style": {
    "navigationStyle": "custom"
  }
}
```

**接口依赖**：
- `GET /user/{user_id}/profile` - 获取用户公开信息
- `GET /community/posts?user_id=xxx` - 获取用户动态
- `POST /community/follow/{user_id}` - 关注用户
- `DELETE /community/follow/{user_id}` - 取消关注

### 11.3 个人资料编辑页 (pages/user/profile.vue)

**功能**：
- 头像上传与预览
- 修改昵称、性别、生日、身高、体重、手机号
- 调用后端 API 保存

**接口依赖**：
- `PUT /user/profile` - 更新用户信息
- `POST /user/avatar` - 上传头像

### 11.4 购物车页面改进 (pages/cart/index.vue)

**修复内容**：
- 修复数据结构不匹配（后端返回 `data.list`，商品信息在 `product.image/price`）
- 修复双返回按钮问题（设置 `navigationStyle: custom`）
- 优化库存检查逻辑

### 11.5 社区用户跳转

**功能**：在社区动态列表中点击用户头像/昵称跳转到他人主页

**实现位置**：`pages/community/index.vue`

```vue
<view class="user-row" @click.stop="viewUserProfile(post.user?.id)">
  <image :src="post.user.avatar" class="avatar" />
  <text class="username">{{ post.user.nickname }}</text>
</view>
```

```typescript
function viewUserProfile(userId?: number) {
  if (!userId) return
  uni.navigateTo({ url: `/pages/user/homepage?id=${userId}` })
}
```

### 11.6 账号与安全页 (pages/user/account-security.vue)

**功能**：
- 展示已绑定手机号（脱敏显示）和微信状态
- 手机号绑定/换绑功能（含验证码发送、倒计时）
- 账号注销功能（二次确认）

**接口依赖**：
- `POST /settings/send-code` - 发送验证码
- `POST /settings/bind-phone` - 绑定手机号
- `POST /settings/delete-account` - 注销账号

### 11.7 隐私设置页 (pages/user/privacy-settings.vue)

**功能**：
- 向医生展示健康数据开关
- 允许陌生人查看动态开关
- 个性化推荐开关
- 设置自动同步到服务器

**接口依赖**：
- `GET /settings` - 获取用户设置
- `PUT /settings` - 更新用户设置

### 11.8 关于我们页 (pages/user/about.vue)

**功能**：
- 展示应用 Logo、名称、版本号
- 功能介绍、用户协议、隐私政策入口
- 检查更新功能

### 11.9 意见反馈页 (pages/user/feedback.vue)

**功能**：
- 反馈内容输入（支持 500 字）
- 图片上传（最多 3 张）
- 联系方式（选填）
- 提交到数据库存储

**接口依赖**：
- `POST /feedback` - 提交反馈

### 11.10 欢迎引导页 (pages/onboarding/welcome.vue)

**功能**：
- 新用户登录后的欢迎引导页面
- 展示应用名称、欢迎语、3D风格插图
- 跳转到基础信息填写页

**路由配置**：
```json
{
  "path": "pages/onboarding/welcome",
  "style": {
    "navigationStyle": "custom",
    "disableScroll": true
  }
}
```

**特点**：
- 自定义导航栏，全屏沉浸式体验
- 禁用页面滚动，固定布局
- 漂浮装饰动画效果

### 11.11 医生在线咨询页 (pages/doctor/chat.vue)

**功能**：
- 与医生进行一对一在线咨询
- 展示医生基本信息（头像、姓名、职称、医院）
- 聊天消息列表（区分用户/医生消息）
- 发送文字消息

**路由配置**：
```json
{
  "path": "pages/doctor/chat",
  "style": {
    "navigationBarTitleText": "在线咨询"
  }
}
```

**接口依赖**：
- `GET /doctor/{id}` - 获取医生详情
- `GET /message/history/{user_id}` - 获取聊天记录
- `POST /message/send` - 发送消息

### 11.12 领券中心页 (pages/coupon/center.vue)

**功能**：
- 展示所有可领取的优惠券
- 按类型筛选（全部/满减/折扣/通用）
- 一键领取优惠券
- 领取成功提示

**路由配置**：
```json
{
  "path": "pages/coupon/center",
  "style": {
    "navigationBarTitleText": "领券中心"
  }
}
```

**接口依赖**：
- `GET /coupon/available` - 获取可领取优惠券
- `POST /coupon/{id}/receive` - 领取优惠券

---

## 12. 页面路由完整列表

| 页面路径 | 导航类型 | 说明 |
|----------|----------|------|
| pages/index/index | TabBar | 首页 |
| pages/sport/index | TabBar | 运动 |
| pages/community/index | TabBar | 社区 |
| pages/shop/index | TabBar | 商城 |
| pages/user/index | TabBar | 我的 |
| pages/login/index | 普通 | 登录页 |
| pages/health/screening | 普通 | 健康筛查 |
| pages/health/result | 普通 | 筛查结果 |
| pages/sport/history | 普通 | 运动记录历史 |
| pages/food/index | 普通 | 饮食记录 |
| pages/community/detail | 普通 | 动态详情 |
| pages/community/publish | 普通 | 发布动态 |
| pages/community/topic | 普通 | 话题详情 |
| pages/shop/detail | 普通 | 商品详情 |
| pages/cart/index | 自定义导航 | 购物车 |
| pages/order/index | 普通 | 订单列表 |
| pages/order/confirm | 普通 | 订单确认 |
| pages/order/detail | 普通 | 订单详情 |
| pages/user/profile | 普通 | 个人资料 |
| pages/user/homepage | 自定义导航 | 他人主页 |
| pages/user/member | 普通 | 会员中心 |
| pages/user/points | 普通 | 积分明细 |
| pages/user/settings | 普通 | 设置 |
| pages/user/account-security | 普通 | 账号与安全 |
| pages/user/privacy-settings | 普通 | 隐私设置 |
| pages/user/about | 普通 | 关于我们 |
| pages/user/feedback | 普通 | 意见反馈 |
| pages/user/member-guide | 自定义导航 | 会员引导页 |
| pages/address/index | 普通 | 地址管理 |
| pages/message/index | 普通 | 消息中心 |
| pages/message/chat | 普通 | 私信聊天 |
| pages/reminder/index | 普通 | 提醒设置 |
| pages/onboarding/welcome | 自定义导航 | 欢迎引导页 |
| pages/doctor/index | 普通 | 远程医疗 |
| pages/doctor/detail | 普通 | 医生详情 |
| pages/doctor/chat | 普通 | 医生在线咨询 |
| pages/doctor/article | 普通 | 健康科普 |
| pages/coupon/index | 普通 | 我的优惠券 |
| pages/coupon/center | 普通 | 领券中心 |
| pages/ai/index | 普通 | AI健康助手 |
| pages/help/index | 普通 | 帮助与反馈 |
| pages/health/index | 普通 | 健康筛查 |
| pages/health/basic-info | 普通 | 基础信息填写 |
| pages/shop/collect | 普通 | 我的收藏 |

